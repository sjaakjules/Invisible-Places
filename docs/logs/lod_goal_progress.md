# LOD Goal Progress

2026-05-27 | Agent 1 | Removed sampled-index preview surface | `src/renderer/pointcloud/PointCloudPreviewState.*`, `src/app/Application.cpp`, `src/renderer/core/VulkanViewportShell.*`, `src/serialization/ProjectDocument.*`, `tests/AssetDiscoveryTests.cpp` | `cmake --build build/macos-debug`; `ctest --test-dir build/macos-debug --output-on-failure` | Passed | Removed resolver/types, sampled-index caches, still-camera sampled-index preparation, interactive sampled-index renderer path, project save/load field, and old resolver test. Added tests for manual point-budget sampling as an explicit cap and legacy serialized preview-mode fields dropping on save. Remaining work: replace export density booleans and HQ EXR naming with explicit adaptive density modes, then implement adaptive hierarchy/draw-item traversal.
2026-05-27 | Agent 2 | Replaced export density boolean with explicit density modes | `src/output/RenderPreset.*`, `src/app/Application.cpp`, `src/renderer/core/VulkanViewportShell.*`, `tests/AssetDiscoveryTests.cpp` | `cmake --build build/macos-debug`; `ctest --test-dir build/macos-debug --output-on-failure` | Passed | Added `PointCloudExportDensityMode` with `FullSource`, `AdaptiveHighQuality`, `MatchViewportAdaptive`, `FastAdaptivePreview`, `ArtisticAsPreview`, and `ArtisticHighQuality`; renamed HQ EXR mode away from sampled-density wording; export requests now carry explicit density mode. Remaining work: implement hierarchy-backed adaptive draw items so non-full-source modes no longer depend on the active point-budget draw count.
2026-05-27 | Agent 3 | Added source-index-preserving adaptive LOD hierarchy scaffold | `src/renderer/pointcloud/PointCloudLodHierarchy.*`, `CMakeLists.txt`, `tests/AssetDiscoveryTests.cpp` | `cmake --build build/macos-debug`; `ctest --test-dir build/macos-debug --output-on-failure` | Passed | Added `PointCloudLodHierarchy`, `PointCloudLodNode`, `PointCloudLodRepresentative`, and `PointCloudDrawItemGpu`, plus traversal tests for `FullSource` source-index correctness and compensated adaptive representatives. Remaining work: upload draw-item buffers and wire viewport/export rendering to `drawItem.sourcePointIndex`.
2026-05-27 | Agent 3 | Built LOD hierarchies for loaded point-cloud sessions and reported adaptive counts | `src/app/Application.cpp` | `cmake --build build/macos-debug`; `ctest --test-dir build/macos-debug --output-on-failure` | Passed | Loaded point-cloud sessions now own `PointCloudLodHierarchy` data and the UI reports adaptive representative count plus represented source count. Remaining work: renderer draw-item buffers and shader fetch through `drawItem.sourcePointIndex`.
2026-05-27 | Agent 4 | Wired adaptive draw items through viewport/export rendering | `src/app/Application.cpp`, `src/renderer/core/VulkanViewportShell.*`, `shaders/pointcloud*.vert`, `shaders/pointcloud*.frag`, `src/serialization/ProjectDocument.*`, `tests/AssetDiscoveryTests.cpp` | `cmake --build build/macos-debug`; `ctest --test-dir build/macos-debug --output-on-failure`; `rg` audits for retired sampled-index symbols | Passed | Viewport and EXR/MP4 export render states now populate adaptive draw items per camera, density mode, style, and viewport/export resolution. Vulkan uploads draw-item buffers, shaders fetch positions, colours, normals, scalar fields, water-effect fields, opacity, emission, and dynamic point-size inputs through `drawItem.sourcePointIndex`, and Full Source remains a raw source path. Removed the unused serialized sampled-index interactive cap and added a std430 layout test for `PointCloudDrawItemGpu`. Active-code audits found no retired sampled-index preview symbols or fixed point-count LOD paths.
2026-05-27 | Agent 5 | Verified Stage 01 adaptive LOD build, tests, and repeatable baseline evidence | `docs/logs/lod_goal_progress.md`, `Saved/diagnostics/lod_compare/lod_compare_metrics.json` | `cmake --build --preset build-macos-debug`; `./build/macos-debug/invisible_places_tests "[pointcloud][lod]"`; `ctest --test-dir build/macos-debug --output-on-failure`; `build/macos-debug/invisible_places.app/Contents/MacOS/invisible_places --lod-compare /Users/juju/Documents/Repositories/Invisible\ Places/Data/Site3-Sample-Terrestrial.ply` | Passed | Build was already current (`ninja: no work to do`), focused LOD tests passed with 20 test cases / 293 assertions, and full `ctest` passed 145/145. The repeatable smaller baseline used `/Users/juju/Documents/Repositories/Invisible Places/Data/Site3-Sample-Terrestrial.ply` (12,183,742 points) and wrote `Saved/diagnostics/lod_compare/lod_compare_metrics.json` with coverage ratio 1, luminance ratio 1.00397, 1,965,951 adaptive representatives, 11,705,070 represented source points, Adaptive High Quality density, and no adaptive fallback. A 100M observational run completed when launched with the absolute path and reported coverage ratio 1, luminance ratio 0.954065, 2,233,029 adaptive representatives, and 100,743,210 represented source points; the first relative-path 100M run timed out waiting for LOD readiness because it did not reuse the existing absolute-path cache hash. Stage 01 diagnostic gaps remain: `lod_compare_metrics.json` does not yet export scene update ms/FPS, frame ms/FPS, draw-item upload ms, draw-item reallocations, persistent/runtime cache status, or peak resident memory; those values are available in the live diagnostics overlay or need a later minimal metrics export improvement.
2026-05-27 | Agent 6 | Completed Stage 02 Fast Basic bounded adaptive core | `src/renderer/pointcloud/PointCloudLodHierarchy.*`, `src/app/Application.cpp`, `src/renderer/core/VulkanViewportShell.*`, `tests/AssetDiscoveryTests.cpp`, `docs/logs/lod_goal_progress.md`, `Saved/diagnostics/lod_compare/lod_compare_metrics.json` | `cmake --build --preset build-macos-debug`; `./build/macos-debug/invisible_places_tests "[pointcloud][lod]"`; `ctest --test-dir build/macos-debug --output-on-failure`; `build/macos-debug/invisible_places.app/Contents/MacOS/invisible_places --lod-compare Data/Site3-Sample-Terrestrial.ply` twice | Passed | Fixed the dropped `maxEstimatedFragments` budget during draw-item emission; unavailable adaptive layers now submit zero points with a waiting/no-visible-reps diagnostic instead of silently falling back to source draw counts, while explicit Full Source paths remain separate. Async viewport traversal now records request generations, cancels superseded work, discards stale completions, and exposes stale-discard/pending-age diagnostics. The sample Fast Basic viewport diagnostic ran 90 moving-camera frames on `Data/Site3-Sample-Terrestrial.ply` (12,183,742 points). Cold/warming run: max submitted/adaptive reps 14,912, representative budget 2,764,800, estimated fragments 410,684, fragment budget 132,710,000, max scene update 11.868 ms, min UI/present FPS 12.506, stale traversals discarded 45, full-source fallback false, budget exceeded false. Warm-cache rerun: same submitted/budget/fragment values, max frame 26.456 ms, max scene update 11.852 ms, min UI/present FPS 37.799, full-source fallback false, budget exceeded false. Beauty comparison still wrote coverage ratio 1, luminance ratio 0.98097, 1,243,659 adaptive representatives, and 12,085,662 represented source points with no adaptive fallback.
2026-05-27 | Agent 7 | Hotfixed Fast Basic zoom-out adaptive reuse and fallback diagnostics | `src/app/Application.cpp`, `src/renderer/core/VulkanViewportShell.*`, `src/renderer/pointcloud/PointCloudLodHierarchy.*`, `tests/AssetDiscoveryTests.cpp`, `docs/logs/lod_goal_progress.md`, `Saved/diagnostics/lod_compare/lod_compare_metrics.json` | `cmake --build --preset build-macos-debug`; `./build/macos-debug/invisible_places_tests "[pointcloud][lod]"`; `ctest --test-dir build/macos-debug --output-on-failure`; `build/macos-debug/invisible_places.app/Contents/MacOS/invisible_places --lod-compare Data/Site3-Sample-Terrestrial.ply` | Passed | Added explicit `requiresAdaptiveDrawItems` gating so adaptive-required layers no longer resolve zero draw items to raw `activePointCount`; Full Source/Beauty Full Source paths remain explicit raw-source paths. Fast Basic now requests `FastAdaptivePreview` during movement and holds that tier while async/fallback is pending, only promoting toward Match Viewport after an exact adaptive set is displayed. Previous-cache reuse is bounded by age and represented-source coverage, with under-covering zoom-in sets replaced by current coarse fallback. Traversal diagnostics now report visible frontier source, emitted source, culled source, cache age, fallback state, and representative/fragment budget pressure. Iteration evidence: the first sample run was bounded but `fast_basic_zoom_out_updated=false` because the diagnostic harness kept orbiting during idle; after holding the camera steady, Fast Basic still promoted into Match while fallback was pending, leaving async in flight; after holding Fast Preview until exact display and raising bounded coarse fallback to 65,536 reps max, the final sample run on `Data/Site3-Sample-Terrestrial.ply` reported `fast_basic_zoom_out_updated=true`, pre/post zoom-out represented source 8,539,120 -> 12,183,742, max submitted/adaptive reps 65,511 under the 86,400 rep budget, estimated fragments 801,527 under the 12,441,600 fragment budget, visible/emitted represented source both 12,183,742, no previous-set reuse, coarse fallback observed, full-source fallback false, budget exceeded false, min UI/present FPS 230.147, and Beauty comparison coverage ratio 1 with luminance ratio 1.01528.
